import { MainTemplate, SettingTemplate } from '@components/templates';
import React from 'react';

const Setting = () => {
  return (
    <MainTemplate title='Pengaturan' subTitle='Home'>
      <SettingTemplate />
    </MainTemplate>
  );
};

export default Setting;
